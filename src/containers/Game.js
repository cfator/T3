import React from 'react'
import { connect } from 'react-redux'

import GameBoard from '../components/GameBoard/GameBoard'

const mapDispatchToProps = (dispatch, ownProps) => ({

});

export default connect(mapDispatchToProps)(GameBoard);